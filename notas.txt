la app queda funcionando, 
hay que corregir varias cosas, por ejem que al elegir o 
tocar uno de los chats sobre temas, en cada pdf, se marque en 
que chat estas, actualmente no sabes si estas en uno o en otro


# Entrar a la carpeta del proyecto
cd ~/brain-api

# Levantar los contenedores (DB + PGAdmin) en segundo plano
docker compose up -d

# Ver si están corriendo bien (Status: Up)
docker ps

# Activar el entorno virtual (Venv)
source venv/bin/activate

# Instalar dependencias nuevas (por si acaso)
pip install python-multipart pypdf sqlalchemy asyncpg

# Lanzar la API con auto-recarga
uvicorn app.main:app --reload

# Ver los logs de la base de datos para ver errores internos
docker compose logs db

# Reiniciar todo el sistema Docker (apagar y prender)
docker compose restart

# Limpiar procesos de Python que quedaron "colgados" (muy útil en Arch)
pkill -f uvicorn



tengo que afianzar lo que aprendi y cambie hoy, agregar la opcion de eliminar chats, renombrarlos, mejorar el rag en si, el sistema de analisis, pulirlo